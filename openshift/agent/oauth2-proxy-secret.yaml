apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secret
  namespace: agenticai-demo
type: Opaque
stringData:
  client-secret: changeme-client-secret        # Keycloak agenticai-agent client secret
  cookie-secret: changeme-32-byte-cookie-secret # Must be 16, 24 or 32 bytes - replace!
