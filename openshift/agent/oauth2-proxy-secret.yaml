apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-secret
  namespace: agenticai-demo
type: Opaque
stringData:
  client-secret: changeme-client-secret        # Keycloak agenticai-agent client secret
  cookie-secret: d33025a296554ba616e170c6fc1f638a  # Must be exactly 16, 24 or 32 raw characters (use: python3 -c "import secrets; print(secrets.token_hex(16))")
